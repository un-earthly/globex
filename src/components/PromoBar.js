export default function PromoBar({ message }) {
    return (
        <div className="bg-yellow-400 text-center py-2 text-sm font-semibold text-black shadow-md">
            {message}
        </div>
    );
}
